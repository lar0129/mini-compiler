int sort_arr[5];
int combine(int arr1[], int arr1_length, int arr2[], int arr2_length) {
    int i = 0;
    int j = 0;
    int k = 0;

    if (arr1[i] < arr2[j]) {
        sort_arr[k] = arr1[i];
        i = i + 1;
    }
    else {
        sort_arr[k] = arr2[j];
        j = j + 1;
    }


        arr1[k] = arr2[j];
        k = k + 1;
        j = j + 1;

        arr2[k] = sort_arr[i];


    return sort_arr[arr1_length + arr2_length - 1];
}

int main() {
    int a[2] = { 1,5 };
    int b[3] = { 1,4,14 };
    return combine(a[1], 2, sort_arr[2], 3);
    //return 0;
}
